// Square Every Digit

// P - Take in a number
// R - A new number that is each digit of the number squared
// E - 9119 => 811181

function squareDigits(num) {
    num.toString();
    num.split(',');
    num = finalNum.forEach(Number(Math.pow(num, 2)))
    num = finalNum.join('');
    return num;
}