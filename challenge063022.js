// 8kyu - Grasshopper: Check for factor

// P - base, factor
// R - true if factor is factor of base : false
// E - (2450, 5) --> true

const checkForFactor = (base, factor) => base % factor == 0 ? true : false