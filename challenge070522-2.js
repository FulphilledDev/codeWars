// 8kyu - Template Strings

// P - obj, feature
// R - string using template string feature
// E - ('Animals', 'Good') ==> 'Animals are Good'

var TempleStrings = function(obj, feature) {
    return `${obj} are ${feature}`
}